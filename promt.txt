You are working on the existing repository:

https://github.com/dynamite-ai-coder/Beta

Your task is to modify the existing client so that it can run reliably on Android Termux, while preserving compatibility with the existing backend and WebSocket protocol.

IMPORTANT:
- Do NOT rewrite the entire project.
- Do NOT change the backend API unless absolutely necessary.
- Preserve the existing WebSocket protocol and task/message formats.
- First inspect the entire repository and understand the current architecture.
- Then modify only the files that actually need changes.
- Keep the project clean and production-oriented.

MAIN GOAL

Convert the current desktop-oriented client into a local Web UI client that runs inside Termux.

The client must start a local HTTP server on:

http://127.0.0.1:24500

When the user opens this address in a browser on the Android device, they should see a modern chat-style interface.

The local web interface replaces the current Tkinter GUI.

REQUIRED ARCHITECTURE

Android
  |
  v
Termux
  |
  +-- Python client
  |
  +-- Local HTTP server :24500
  |
  +-- WebSocket connection to backend
  |
  +-- Browser automation worker
  |
  +-- File upload/download
  |
  +-- Local chat UI

1. REPLACE TKINTER

The current client uses Tkinter in client/main.py.

Remove the dependency on Tkinter from the Termux version.

There must be no requirement for a desktop GUI.

The user should interact with the client through the browser.

Instead of Tkinter dialogs, provide equivalent functionality through the web interface.

2. LOCAL WEB UI

Create a lightweight local web application.

Use FastAPI/Starlette or another lightweight Python HTTP framework that fits the existing project.

The UI must be available at:

http://127.0.0.1:24500

Create a clean responsive interface suitable for an Android phone.

The interface should contain:

CHAT AREA
- message history
- user messages
- client/system messages
- backend/task status messages
- timestamps
- scrolling history

INPUT AREA
- text input
- Send button
- Enter-to-send
- multiline support if practical

FILE SUPPORT
- file picker
- upload button
- uploaded file list
- download links for generated/received files
- file names and sizes
- configurable upload size limits

Files must be stored in a configurable local directory.

Do not expose arbitrary filesystem paths through the HTTP API.

3. CLIENT CONFIGURATION

Support:

BACKEND_URL
CLIENT_ID
CLIENT_TOKEN
BROWSER_HEADLESS
BROWSER_PROFILE_PATH
TASK_TIMEOUT
LOG_LEVEL

Environment variables must remain supported.

If practical, add a Settings section in the web UI.

Never display secrets such as CLIENT_TOKEN unnecessarily.

4. BACKEND CONNECTION

Preserve the existing backend communication.

The client must continue to connect to:

/api/clients/ws/agent

using the existing WebSocket protocol.

Preserve:
- client registration
- CLIENT_ID
- CLIENT_TOKEN
- CLIENT_REGISTER
- READY
- heartbeat
- reconnection
- TASK_ASSIGNED
- TASK_CANCEL
- TASK_STARTED
- TASK_PROGRESS
- TASK_RESULT
- TASK_ERROR

Do not invent a new incompatible protocol.

The local Web UI should expose the state of the existing client.

5. REAL-TIME UI

Use WebSocket or SSE so that the browser UI updates immediately.

Display:
- backend connection status
- task started
- task progress
- task completed
- task failed
- browser status
- client logs

6. BROWSER AUTOMATION

Inspect client/browser.py carefully before modifying it.

The current implementation uses Selenium and Chrome.

Determine what is actually possible on Android Termux.

Do NOT assume that desktop ChromeDriver will work on Android.

Implement the most appropriate Termux-compatible browser architecture.

If Selenium + Chromium can be supported reliably, implement it properly.

If the existing browser implementation cannot work directly on Android, isolate the browser backend behind a clean interface so that the Web UI/client can still run without browser automation.

Support:

BROWSER_ENGINE=selenium

Do not remove existing browser functionality without documenting why.

7. BROWSER WORKER

Preserve the existing BrowserWorker API where possible:

- start
- stop
- restart
- navigate
- get_dom_elements
- find_element_by_selector
- type_text
- click_element
- take_screenshot
- get_current_url
- get_page_source
- detect_captcha

Make sure these functions do not block the asyncio event loop.

8. SCREENSHOT

Implement the screenshot action properly.

When a screenshot is requested:

1. Capture it from the browser worker.
2. Save it to a controlled local directory.
3. Make it available to the Web UI.
4. If required by the existing backend protocol, send an appropriate task result/event.

Do not expose arbitrary filesystem files.

9. TASK CANCELLATION

Improve TASK_CANCEL handling.

When the backend sends TASK_CANCEL:

- identify the running task
- cancel/stop its asyncio task
- prevent additional browser actions
- return an appropriate task state/result
- clean up resources if necessary

Do not terminate the entire client.

10. TASK TIMEOUT

Use TASK_TIMEOUT correctly.

A task that exceeds the configured timeout must be stopped safely.

Report a timeout as TASK_ERROR or the appropriate existing protocol message.

11. MULTIPLE TASKS

Inspect BrowserWorkerManager.

Do not assume max_workers currently provides multiple browsers.

Implement concurrency correctly if safe.

Support:

MAX_BROWSER_WORKERS=1

Default to 1 for Android/Termux.

Each browser worker must have isolated state/profile when multiple workers are enabled.

12. SECURITY

The HTTP server must bind ONLY to:

127.0.0.1

Do not bind to 0.0.0.0 by default.

Protect against:
- path traversal
- arbitrary file reads
- arbitrary file writes
- command execution through uploaded files
- unsafe filenames
- oversized uploads
- malformed WebSocket messages
- invalid task IDs
- unauthorized filesystem access

Never execute uploaded files.

Sanitize uploaded filenames.

Store uploads in a dedicated directory.

13. LOCAL API

Create clean local endpoints such as:

GET /
GET /api/status
GET /api/config
POST /api/chat
POST /api/upload
GET /api/files
GET /api/files/{file_id}
DELETE /api/files/{file_id}

You may change the endpoint names if a better architecture fits the project.

Document every endpoint.

14. LOCAL WEBSOCKET

Create a local WebSocket endpoint such as:

/ws

It should push:
- connection status
- backend connection status
- task events
- browser status
- logs
- chat events
- file events

The Android browser should automatically reconnect if disconnected.

15. LOGGING

Keep normal console logging because Termux users need to see logs.

Also expose safe application logs through the Web UI.

Never expose:
- CLIENT_TOKEN
- passwords
- authorization headers
- sensitive credentials

in the web UI logs.

16. CREDENTIALS

Be careful with credentials.

Do not log passwords.

Do not display passwords in normal logs.

Do not unnecessarily persist credentials.

If the existing task system requires credentials, preserve the existing behavior while minimizing exposure.

17. REMOVE DESKTOP ASSUMPTIONS

Search the whole client for:
- tkinter
- Windows-only APIs
- Windows paths
- desktop-only assumptions
- hardcoded Chrome paths
- hardcoded Windows user-agent assumptions
- GUI requirements

Replace them with portable implementations where appropriate.

18. DEPENDENCIES

Inspect:
- requirements.txt
- pyproject.toml
- setup configuration

Add only dependencies actually required.

Avoid unnecessarily large frameworks.

The final client should be practical to install in Termux.

Provide the correct Termux installation procedure.

Do not invent Termux package names.

If browser automation requires a separate Android/Termux component, document it clearly.

19. STARTUP

The final client should preferably start with:

python -m client

On startup print:

====================================
 Beta Android / Termux Client
====================================

Web UI:
http://127.0.0.1:24500

Backend:
...

Client ID:
...

WebSocket:
CONNECTED / DISCONNECTED

Browser:
READY / NOT AVAILABLE

20. NO TKINTER

This is mandatory.

The final Termux client must not require Tkinter.

21. BACKWARD COMPATIBILITY

Keep existing client functionality where possible.

Prefer modifying:

client/main.py
client/config.py
client/browser.py
client/websocket_client.py

and adding modules such as:

client/webapp.py
client/static/
client/templates/

only when needed.

Do not modify unrelated backend code unless absolutely necessary.

22. CODE QUALITY

Use:
- type hints
- async/await correctly
- structured logging
- proper exception handling
- small functions
- clear module boundaries
- no duplicated HTTP client implementations where they can be unified
- no hardcoded secrets
- no unnecessary global state

Avoid blocking calls inside async functions.

23. TESTING

After modifications:

1. Run Python syntax checks.
2. Run import checks.
3. Start the local HTTP server.
4. Verify:

http://127.0.0.1:24500

loads successfully.

5. Test WebSocket connection to the backend.
6. Test file upload.
7. Test file listing/download.
8. Test chat messages.
9. Test backend connection loss and automatic reconnect.
10. Test task cancellation.
11. Test task timeout.
12. Test graceful shutdown.

If Android/Termux browser automation cannot be tested in the current environment, do not fake a successful test. Clearly report which part could not be tested.

24. REQUIRED WORKFLOW

Before editing:

1. Inspect the complete repository.
2. Inspect all files under client/.
3. Inspect the backend API/WebSocket implementation.
4. Inspect dependency configuration.
5. Identify exactly which files need changes.
6. Briefly explain the planned changes.
7. Then implement them.

After implementation:

1. Show the files changed.
2. Show the files added.
3. Explain the new architecture.
4. Provide exact Termux installation commands.
5. Provide exact startup commands.
6. Provide environment-variable examples.
7. Report test results.
8. Report Android-specific limitations.

Do not stop after creating a plan.

Actually implement the changes in the repository.

The final result must be a usable Android Termux client with a local browser-based UI on:

http://127.0.0.1:24500

while retaining compatibility with the existing Beta backend and WebSocket protocol.