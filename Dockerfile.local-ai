FROM python:3.12-slim

ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1

RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential \
    curl \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app

# Install llama-cpp-python with CPU only
RUN pip install --no-cache-dir "llama-cpp-python>=0.3.0" httpx fastapi uvicorn

# Pre-download Phi-3-mini-4k-instruct model on build
RUN mkdir -p /app/models && \
    python3 -c "
import httpx, os
url = 'https://huggingface.co/bartowski/Phi-3-mini-4k-instruct-GGUF/resolve/main/Phi-3-mini-4k-instruct-Q4_K_M.gguf'
target = '/app/models/phi-3-mini-4k-instruct-q4_k_m.gguf'
if not os.path.exists(target):
    print(f'Downloading Phi-3-mini-4k-instruct Q4_K_M...')
    with httpx.Client(timeout=600, follow_redirects=True) as c:
        with c.stream('GET', url) as r:
            r.raise_for_status()
            with open(target, 'wb') as f:
                for chunk in r.iter_bytes(65536):
                    f.write(chunk)
    print(f'Done: {os.path.getsize(target) // (1024*1024)}MB')
"

COPY local_ai_server.py .
COPY shared_protocol.py .

EXPOSE 8100

ENV LOCAL_AI_ENABLED=true
ENV LOCAL_AI_MODEL=/app/models/phi-3-mini-4k-instruct-q4_k_m.gguf
ENV LOCAL_AI_CONTEXT=4096
ENV LOCAL_AI_MAX_TOKENS=512
ENV LOCAL_AI_THREADS=4

HEALTHCHECK --interval=30s --timeout=10s --start-period=60s --retries=3 \
    CMD curl -f http://localhost:8100/health || exit 1

CMD ["python3", "local_ai_server.py"]
